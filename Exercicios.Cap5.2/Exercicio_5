'''
5. Faça uma função que receba a taxa de mortalidade de cada país (Deathrate) e retorne o texto 
'Balanced' caso o valor seja < 9 e 'Urgent' caso contrário. Em seguida, crie um campo no Dataset 
chamado 'Humanitarian Help' que recebe estes valores para cada país. No final, mostre o Dataset para 
verificar se a inserção da nova coluna foi feita com sucesso.
'''

#Recebendo a taxa de mortalidade
taxa_mortalidade = dataset['Deathrate']

#Função para classificar a mortalidade
def classifica_mortalidade(taxa_mortalidade):
  if taxa_mortalidade < 9:
    return 'Balanced'
  else:
    return 'Urgent'

#Inserção da nova coluna no dataset
dataset['Humanitarian Help'] = dataset['Deathrate'].apply(classifica_mortalidade)

#Saída de dados
print(dataset)
