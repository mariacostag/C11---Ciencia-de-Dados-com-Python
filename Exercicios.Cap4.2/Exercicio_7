"""
7. Quantas missões foram lançadas a partir de localizações que contêm
"Russia" (coluna Location)?
"""

import numpy as np

#Separar os dados
dados = dataset[1:]

# Coluna 2 é a colune da location
localizacao = dados[:, 2]

# Verificar quais localizações contêm "Russia"
missoes_russia = np.char.find(localizacao, 'Russia') >= 0

# Contar quantas missões foram lançadas de locais com "Russia"
total_russia = missoes_russia.sum()

print(f'Missões lançadas a partir de localizações que contêm "Russia": {total_russia}')
